[project]
name = "py-pieces"
version = "2025.8.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.13.0",
    "orjson>=3.11.2",
    "polars>=1.32.3",
    "ruff>=0.12.9",
]

[tool.ruff]
unsafe-fixes = true

[tool.ruff.lint]
select = [
    "E4", "E7", "E9", "F", # default rules
    "B", "A", # flake8 rules
    "I", # isort rules
]
