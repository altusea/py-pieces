[project]
name = "py-pieces"
version = "2025.9.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "anyio>=4.10.0",
    "mcp[cli]>=1.13.1",
    "orjson>=3.11.3",
    "polars>=1.33.0",
    "ruff>=0.12.12",
    "scikit-learn>=1.7.1",
    "trio>=0.31.0",
]

[tool.ruff]
unsafe-fixes = true

[tool.ruff.lint]
select = [
    "E4", "E7", "E9", "F", # default rules
    "B", "A", # flake8 rules
    "I", # isort rules
]
